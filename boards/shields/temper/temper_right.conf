# Deep sleep after 30 minutes idle (default is 900000 = 15 min).
# Each half sleeps independently, so this must be set on both.
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# Mouse move / scroll / click, used by the Miryoku Mouse and Button layers
CONFIG_ZMK_POINTING=y
